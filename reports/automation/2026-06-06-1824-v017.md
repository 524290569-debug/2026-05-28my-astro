# 网站自动优化报告

## 基本信息

- 运行时间：2026-06-06 18:24:25 +08:00
- 版本号：v017
- 运行方式：自动
- 当前分支：main
- 自动化记忆路径：C:\Users\52429\.codex\automations\astro\memory.md
- 运行前状态：`git status --short --branch` 显示 `main...origin/main`，工作区干净。

## 本次检查到的问题

- `src/components/CyberpunkHero.astro` 中透明 Hero 层的尺寸 metadata 与真实 WebP 尺寸不一致：`city-v5.webp` 实际为 `1600x556`，`rooftop-v5.webp` 实际为 `1600x267`，组件中仍为小尺寸占位值。
- 首屏 Hero 三张关键图只在 body 内通过 `img` 被浏览器发现，head 中没有显式 preload；对雨夜首屏场景来说，资源优先级还可以更明确。
- 本轮仍需使用 `C:\Users\52429\.codex` 作为 `$env:CODEX_HOME` 为空时的回退路径，并显式使用 UTF-8 读取中文规范。
- 命令层遇到一次 `rg` 正则引号解析错误，已改用 `Select-String -SimpleMatch` 检查 `dist/index.html`。
- Browser 首次字符串形式 `evaluate` 未返回指标，已改用函数形式重试并通过。
- Browser 插件出现一次外部 telemetry POST 超时，但页面 DOM 复检数据正常返回，不影响本轮网站验证。
- 现有 `tools/capture_views.mjs` 截图后仍残留 4327 预览进程和本轮 Edge profile，已手动关闭并清理。

## 本次优化建议

- Hero 关键图片加载链路加固：修正真实图片尺寸，并在 head 中预加载 Hero 三层图。影响是首屏图片发现更早、metadata 更准确；风险低，需观察是否增加首屏请求压力。
- 下方分区渲染节流：评估 `content-visibility` 与 `contain-intrinsic-size`。影响是降低非首屏渲染成本；风险中，可能影响锚点定位和截图。
- 移动端截图覆盖补强：增加 mobile contact / reading-card 截图点。影响是验证更完整；风险低，但 `tools/` 当前是本地忽略目录。
- 雨墙纹理加载范围收敛：`rain-wall.webp` 约 120KB，可评估进一步压缩或限制使用范围。影响是资源更轻；风险中，可能损失潮湿墙面质感。
- 键盘焦点状态统一：补齐更多链接的 focus 反馈。影响是可访问性更稳；风险低，但视觉变动分散。

## 本次实际修改

- 采纳建议：Hero 关键图片加载链路加固。
- `BaseLayout` 新增 `preloadImages` prop，支持页面把关键图片写入 head preload。
- 首页向 `BaseLayout` 传入 `sky-v5.webp`、`city-v5.webp`、`rooftop-v5.webp` 三张 Hero 图，其中城市层使用 `fetchpriority="high"`。
- `CyberpunkHero` 修正城市层和屋顶层的 `width` / `height` 属性为真实 WebP 尺寸。
- 未新增动画、未新增依赖、未新增资源。

## 修改文件

- `src/layouts/BaseLayout.astro`
- `src/pages/index.astro`
- `src/components/CyberpunkHero.astro`
- `reports/automation/2026-06-06-1824-v017.md`

## 新增资源

- 无。

## 构建结果

- `ASTRO_TELEMETRY_DISABLED=1; npm.cmd run build`：通过。
- `tools/capture_views.mjs` 内部二次构建：通过。
- `dist/index.html` 检查：已生成 3 条 Hero image preload，且 Hero 图片输出尺寸为 `1600x686`、`1600x556`、`1600x267`。

## 截图结果

- `screenshots/auto-2026-06-06-1820-v017-home.png`
- `screenshots/auto-2026-06-06-1820-v017-about.png`
- `screenshots/auto-2026-06-06-1820-v017-works.png`
- `screenshots/auto-2026-06-06-1820-v017-blog.png`
- `screenshots/auto-2026-06-06-1820-v017-blog-notes.png`
- `screenshots/auto-2026-06-06-1820-v017-contact.png`
- `screenshots/auto-2026-06-06-1820-v017-mobile-home.png`
- `screenshots/auto-2026-06-06-1820-v017-mobile-blog-notes.png`

## 视觉判断

- 桌面 Hero、Works、Blog、Contact 截图目视无文字重叠、无明显视觉拥挤，图片加载正常。
- 移动端 Hero 与 notes 截图目视正常，Hero 电台面板在 390px 宽度下未溢出。
- Browser DOM 复检：桌面 `scrollWidth=clientWidth=1265`，移动端 `scrollWidth=clientWidth=375`，可见图片失败数为 0。
- Browser DOM 复检：head 中存在 3 条 Hero preload；Hero 图片自然尺寸与属性尺寸一致。

## 性能判断

- 本轮新增 3 条 Hero preload，总资源约 176KB，均为已有首屏图片；没有新增脚本依赖和持续动画。
- 城市层使用 `fetchpriority="high"`，其余两层保持 `auto`，避免所有首屏图同时抢占高优先级。
- 性能风险低；下一轮可继续观察首屏请求瀑布，避免 preload 数量继续膨胀。

## 下一次优化方向

- 优先加固本地截图流程：让 `tools/capture_views.mjs` 在正常和异常退出时稳定关闭 preview / Edge，并增加 mobile contact / reading-card 截图点；该项建议保持本地工具属性，避免把 `tools/` 重新公开提交。
- 评估是否给下方内容分区加入轻量渲染约束，但需先验证锚点跳转和移动端截图定位。

## Git 提交

- 提交信息：`Auto optimize website v017`
- 提交号：待本报告随本轮改动提交后由 Git 生成。
- 推送结果：待本轮提交后推送；若失败，将在自动化记忆和最终回复中说明。
